import React from 'react';

/**
 * @deprecated This component is no longer used as of the direct-to-print update.
 * It is now an empty component that renders nothing.
 */
const ConfirmationModal: React.FC = () => {
  return null;
};

export default ConfirmationModal;